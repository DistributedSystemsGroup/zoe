#!/bin/bash

. utils/base.sh

echo '<============================================================================>'
for id in `cat state.zoe`; do
	docker -H $SWARM logs $id
	echo '<============================================================================>'
done

